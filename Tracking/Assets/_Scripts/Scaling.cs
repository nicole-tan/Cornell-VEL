using UnityEngine;
using System.Collections;

public class Scaling : MonoBehaviour {

	//Depending on location of hmd, you can guess the height of the person
		//Find the location of the hmd (aka y distance; distance from ground)
	//Pull data from a source to determine the arm length
		//Arm length data 
	//Adjust the arm length accordingly 

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
